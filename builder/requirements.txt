# Python deps for the worker image. Most are installed by the Dockerfile
# (torch, fastai, ffmpeg). This file lists the additional ones required
# by handler.py at runtime.
#
# Note — we do NOT pin torch/torchvision here; those are baked by the base
# runpod/pytorch image and pinned via the FROM tag.

requests>=2.31        # for input download (urllib works too, but requests is cleaner)
Pillow>=10.0          # for the DeOldify colorizer output (saved PIL.Image)